import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, FileText, Plus } from "lucide-react";
import { DeletePostButton } from "@/components/admin/delete-post-button";
import { getPosts } from "@/lib/blog";

export default async function PostsPage(){const posts=await getPosts();const published=posts.filter((post)=>post.status==="published").length;return <><div className="flex flex-col gap-5 border-b border-black/10 pb-8 sm:flex-row sm:items-end sm:justify-between"><div><p className="mono text-[9px] tracking-[.18em] text-peak-blue" style={{color:"var(--blue)"}}>MAIN WEBSITE · JOURNAL</p><h1 className="mt-3 text-4xl font-bold sm:text-6xl">Blog Posts</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-black/50">Main Landing Page မှာ ဖတ်စရာ content ရေးပါ။ Draft က public မမြင်ရဘဲ Published လုပ်ထားတာပဲ website မှာပေါ်ပါမယ်။</p></div><Link href="/website/posts/new" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-[#07131c] px-5 text-sm font-bold !text-white" style={{color:"#fff"}}><Plus size={17}/>Post အသစ်ရေးမယ်</Link></div><div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-black/8 bg-white p-5"><p className="text-xs text-black/45">Post အားလုံး</p><strong className="mt-2 block text-3xl">{posts.length}</strong></div><div className="rounded-2xl border border-black/8 bg-white p-5"><p className="text-xs text-black/45">Published</p><strong className="mt-2 block text-3xl text-emerald-600">{published}</strong></div><div className="col-span-2 rounded-2xl border border-black/8 bg-white p-5 sm:col-span-1"><p className="text-xs text-black/45">Draft</p><strong className="mt-2 block text-3xl">{posts.length-published}</strong></div></div><section className="mt-6 overflow-hidden rounded-2xl border border-black/8 bg-white"><div className="flex items-center justify-between border-b border-black/8 px-5 py-4"><h2 className="text-sm font-bold">ရေးထားတဲ့ Posts</h2><a href="https://project-peak-landing.vercel.app/journal" target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-xs font-bold text-black/50">Website မှာကြည့်မယ် <ArrowUpRight size={14}/></a></div>{posts.length?<div>{posts.map((post)=><article className="grid gap-4 border-b border-black/7 p-5 last:border-0 sm:grid-cols-[72px_1fr_auto] sm:items-center" key={post.id}><div className="relative h-16 w-full overflow-hidden rounded-xl bg-[#07131c] sm:w-18">{post.cover_image_url?<Image src={post.cover_image_url} fill sizes="72px" className="object-cover" alt=""/>:<span className="grid h-full place-items-center text-white/45"><FileText size={20}/></span>}</div><div><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-2 py-1 text-[9px] font-bold ${post.status==="published"?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-700"}`}>{post.status.toUpperCase()}</span>{post.featured?<span className="rounded-full bg-sky-50 px-2 py-1 text-[9px] font-bold text-sky-700">FEATURED</span>:null}</div><h3 className="mt-2 text-lg font-bold">{post.title}</h3><p className="mt-1 text-xs text-black/40">/{post.slug} · {post.language==="mm"?"မြန်မာ":"English"} · {new Date(post.updated_at).toLocaleDateString("en-GB")}</p></div><div className="flex items-center gap-2"><Link href={`/website/posts/${post.id}`} className="inline-flex min-h-10 items-center rounded-lg border border-black/10 px-4 text-xs font-bold">ပြင်မယ်</Link><DeletePostButton id={post.id}/></div></article>)}</div>:<div className="grid min-h-56 place-items-center p-8 text-center"><div><FileText className="mx-auto text-black/20"/><strong className="mt-3 block">Post မရှိသေးပါ</strong><p className="mt-2 text-sm text-black/40">ပထမဆုံး Project Peak Journal post ကို စရေးပါ။</p></div></div>}</section></>}
